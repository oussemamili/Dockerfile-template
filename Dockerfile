FROM {{BASE_IMAGE}}

ENV {{ENV_VARS.key}}={{ENV_VARS.value}}

WORKDIR {{WORKDIR}}

COPY {{FILES.src}} {{FILES.dest}}

RUN {{INSTALL_COMMAND}}

EXPOSE {{PORT}}

ENTRYPOINT [{{ENTRYPOINT}}]

CMD [{{CMD}}]
