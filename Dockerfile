FROM {{BASE_IMAGE}}

ENV {{ENV_VARS_KEY}}={{ENV_VARS_VALUE}}

WORKDIR {{WORKDIR}}

COPY {{FILES_SRC}} {{FILES_DEST}}

RUN {{INSTALL_COMMAND}}

EXPOSE {{PORT}}

ENTRYPOINT [{{ENTRYPOINT}}]

CMD [{{CMD}}]
